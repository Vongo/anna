library(shiny)
shinyUI(
	fluidPage(
		titlePanel("ANNA"),
		sidebarLayout(
			position="left",
			mainPanel(
				tabsetPanel(
					id="tabs",
					tabPanel(
						title="User Detail",
						fluidRow(
							column(2,
								textInput("userName","")
							),
							column(1,
								actionButton("validateUserName", "Ok !")
							)
						)
					),
					tabPanel(
						title="Chat",
						fluidRow(
							column(8,
								htmlOutput("history")
							)
						),
						fluidRow(
							column(4,
								htmlOutput("input")
							),
							column(1,
								actionButton("userSpoke", "Speak")
							),
							column(5,
								htmlOutput("evaluation")
							)
						)
					)
				)
			),
			sidebarPanel(
				uiOutput("temp")
			)
		)
	)
)
